schedule: '"* * * * *"' # Runs every mins.
vault:
  address: {{ vault_url }}
  shards:
    - "{{ vault_unseal_key_1.stdout }}"
    - "{{ vault_unseal_key_2.stdout }}"
    - "{{ vault_unseal_key_3.stdout }}"